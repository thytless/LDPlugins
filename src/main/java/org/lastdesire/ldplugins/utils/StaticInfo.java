package org.lastdesire.ldplugins.utils;

import org.bukkit.ChatColor;

public class StaticInfo {
    public static final String PREFIX = ChatColor.BOLD + "[" + ChatColor.AQUA + "LDPlugins" + ChatColor.WHITE + ChatColor.BOLD + "] " + ChatColor.RESET;

    public static final String ERR_NAP = PREFIX + ChatColor.RED + "You must be a player!";

    public static final String ERR_ARGU = PREFIX + ChatColor.RED + "Too many arguments!";


}
